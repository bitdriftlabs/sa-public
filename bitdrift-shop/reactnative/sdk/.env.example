# bitdrift API key — get yours at https://app.bitdrift.io
BITDRIFT_API_KEY=your_api_key_here

# Override only if pointing at a non-default bitdrift instance (e.g. on-premise)
# BITDRIFT_API_HOST=api.bitdrift.io

# Backend port (default: 5173)
# BACKEND_PORT=5173
